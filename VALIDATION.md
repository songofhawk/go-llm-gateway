# 验收记录

验收日期：2026-09-24。环境：macOS arm64 / Apple M2 Pro，Go 1.26.8。

测试使用官方 Go 工具链，并在执行前校验安装包的 SHA-256。

## 验证内容

所有网络测试都使用本地临时 HTTP 模拟供应商，任务测试使用临时 SQLite 文件。没有调用付费模型、读取真实供应商凭据或改动业务数据库。

| 层次 | 验证点 |
| --- | --- |
| 第一课代理 | JSON/流式转发、首块及时刷新、路径/方法限制、认证替换、重定向拒绝、请求大小限制 |
| 模型路由 | 三档映射、保留额外请求字段、同组负载分配、候选组 fallback、4xx 不盲目重试 |
| 错误释放 | 客户端取消、响应头/读取空闲/单次/总时限、非法 JSON、响应超限、空流、body 关闭和名额归还 |
| 流协议 | 开始输出后不 fallback、缺少 DONE 报截断、跨块/多行/CRLF/CR 终止事件、DONE 后不透传多余字节 |
| 并发 | 同时 256 次调用不突破供应商容量 8；长流保持占位；慢客户端写超时取消上游；关机取消活动流 |
| 异步任务 | 202 提交→真实模型适配调用→SQLite 保存→后处理→HTTP 查询；固定 worker、容量拒绝、失败状态 |
| 持久恢复 | 关闭并重新打开数据库恢复；结果写入故障不虚报完成；后处理故障恢复不再调用模型 |

## 命令

```sh
go test -timeout 60s -count=1 ./...
go test -timeout 60s -race -count=1 ./...
go vet ./...
go build ./...
go test -run '^$' -bench BenchmarkGatewayParallel -benchtime=10000x -benchmem ./internal/gateway
```

最终结果：普通测试与竞态测试全部通过，共 37 项顶层测试（网关 21、持久任务 11、第一课 5，另含子用例）。`go vet ./...`、`go build ./...` 均通过；格式检查和 `git diff --check` 通过。命令入口由构建覆盖，完整网关行为由真实 HTTP/SQLite 集成测试覆盖，没有启动常驻服务。

## 微基准

内存 provider、10000 次调用、10 个并行执行单元的一次测量：约 1632 ns/op，1916 B/op，23 allocs/op。这里不包含真实网络、模型推理或数据库；只能用于理解路由本身的开销，不能换算为生产网关的模型吞吐保证。

## 实际发现并修正的问题

- Go 的 JSON 解码将 `null` 写入 string 时不会报错，因此增加了 model:null 的显式校验。
- SSE 结束判断原先只看单行最后一个 data 值；改成以完整事件判断，处理多行和跨块，且不发送终止标记之后的字节。
- 增加 HTTP body 读取前的入口容量限制，以及认证拒绝等路径也受约束的服务器 ReadTimeout。
- 测试模拟服务若不读取 HTTP 请求 body，就等待 context 取消，会使 HTTP/1 断开检测本身卡住。修正模拟服务先消费请求，真实取消/清理测试随后通过；这是测试环境构造问题，与网关功能错误分开记录。

## 范围限制

未做真实付费 provider 账号联调、跨机器压测、生产部署或多副本验证。竞态检测证明本轮执行路径没有报告数据竞争，不证明所有未来输入都没有 bug。普通请求与长流的内存预算由配置决定，生产应配合压测调整。

## 后续：Mock 与 pprof 压力验证

新增七场景压力采样、独立本机 pprof 入口和 Mock 故障注入，详见 [实测报告](docs/STRESS-REPORT.md)。最终 49 项自动测试及 race 通过；实际高频取消压测仍存在连接异常，不将其计为通过。错误体有界排空和 503 映射已依据压力结果修正。

## 第 7 步：请求速率限流

新增 9 项顶层测试（包含错误路径子用例），全项目累计 58 项。完整 `go test -timeout 60s -race -count=1 ./...`、`go vet ./...` 和构建通过。

覆盖令牌桶精确补充与突发上限、512 个并发争抢 37 个令牌、非法配置、429/Retry-After、鉴权顺序、聊天和异步提交共享额度、查询豁免、失败不退款、fallback 只扣一次，以及已有长流继续完成。测试使用受控时钟和临时本机 HTTP/SQLite，不调用真实模型。

第 6 步正式纳入学习路线，原压测结果与未解决的取消连接异常均保留。第 7 步的 `-rate/-burst` 默认为 0，不改变第 6 步关闭请求速率限制时的实验条件。

## 图文教程验证

第 0～7 步与项目总览共新增 17 张 Mermaid 图，均通过 Mermaid CLI 11.12.0 实际渲染并导出 SVG。插图包括调用时序、取消与资源所有权、模型路由、任务状态、压测角色和令牌桶；SVG 已设置明确尺寸，中文显示及关键流程布局经过浏览器截图检查。

已检查图源与 SVG 一一对应、XML 可解析、教程图片与本地链接存在，并清理不适合公开教程的内部语境。本轮只修改文档及图形构建工具，没有修改网关行为，因此没有重复运行应用压测。第 6 步的实际压力测试结论与未解决问题仍保留。
