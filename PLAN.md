# Go 大模型网关：实施计划

目标：从零学习 Go，通过可以独立运行的第一课，再逐步阅读完整网关，理解长连接和异步任务的资源边界。项目通过本地 Mock 独立运行，示例配置不包含真实账号或业务数据。

## 核心抽象

- 模型档次：cheap / balanced / powerful 与真实模型 ID 分离，通过配置映射。
- Provider：封装供应商协议、认证和 HTTP 客户端复用。
- 调用编排：在容量和时间预算内选择候选，执行显式 fallback。
- 后台任务：分离提交、执行、结果持久化和后处理。

## 实施顺序和验收门槛

| 阶段 | 实现 | 学习重点 | 验收 |
| --- | --- | --- | --- |
| 01 | 独立最小 HTTP 代理，支持 JSON / SSE | package、struct、error、defer、HTTP handler | 请求转发、流式逐块输出、断开取消 |
| 02 | Provider 接口、三档模型映射、显式 fallback | interface、组合、context | 模型映射、429/5xx 切换、4xx 不重试、开始输出后不切换 |
| 03 | 有界并发、最少在途负载均衡、超时、优雅退出 | mutex、channel、goroutine、资源所有权 | 长流占用槽位、容量耗尽拒绝、取消/超时释放、race 测试 |
| 04 | SQLite 持久化队列、固定 worker、先保存再处理 | database/sql、事务、状态机、至少一次 | 重启恢复、队列上限、结果保存失败、处理重试与幂等 |
| 05 | 故障实验与设计复盘 | 从可观察结果证明设计 | 成功、边界、故障实验入口 |
| 06 | Mock LLM + pprof 压测 | CPU、堆、协程、阻塞与锁画像 | 七场景原始采样，保留未解决连接异常 |
| 07 | 全局令牌桶请求速率限流 | rate、burst、并发原子性、429 | 补充/突发/并发边界、HTTP 与长流集成 |

每阶段补充中文教程与故障实验。第一阶段保留独立入口；后续阶段在小型完整实现上递进，避免维护四份近似代码。实现完成后更新本文件状态和验收证据。

## 关键设计约束

1. 只实现 OpenAI Chat Completions 兼容协议的 provider adapter，保留扩展接口；不声称兼容原生 Bedrock、Vertex、Anthropic 协议。
2. 长流不能全量缓冲。并发槽位必须覆盖整个响应体读取期，不能在收到 HTTP 头时释放。
3. 总时限、上游响应头时限、流读取空闲时限、下游写时限分别控制。客户端断开取消上游。
4. fallback 有限次数，每个候选最多一次；响应发出后绝不切换，避免拼接两个模型答案。
5. 同步与异步分别限制入口并发；后台任务不继承已结束的提交请求 context。
6. 数据库是任务事实来源，内存通知只负责唤醒。状态转换要持久化；重启可能重复调用供应商，不能承诺 exactly-once。
7. 不加入 Redis、Kafka、服务发现、ORM 或复杂框架。单进程 SQLite 是教学取舍，分布式租约留作扩展。
8. 已知模型档次、容量和状态使用确定性规则；语义自动分档作为可选扩展，不成为基础调用链的依赖。

## 状态

- [x] 项目范围与分阶段计划
- [x] 01 最小代理
- [x] 02 模型路由与 fallback
- [x] 03 并发与生命周期
- [x] 04 持久化异步任务
- [x] 教程、故障注入、完整验收

验收完成：37 项顶层测试通过（另含状态码、超时与协议子用例），`go test -race`、`go vet`、`go build` 通过。完整证据和适用边界见 [VALIDATION.md](VALIDATION.md)。

## 第 6 步：Mock + pprof

- [x] 独立 Mock LLM，配置生成长度、速率、故障与停流
- [x] 本机独立 pprof 端口与有界压力客户端
- [x] 七类负载实际采样、资源排空与原始证据
- [x] 依据压测修正错误响应连接复用及 503 映射
- [ ] 高频取消时的本机连接异常根因（保留为已发现问题，见压测报告）

## 第 7 步：请求速率限流

- [x] 明确第 3 步仅限制并发，第 7 步补充请求速率
- [x] 无额外依赖的全局令牌桶，rate/burst 参数及 429/Retry-After
- [x] 聊天和异步提交共用额度；已开始流、任务查询不重复扣令牌
- [x] 中文教程、确定性时钟测试和真实 HTTP/SQLite/SSE 集成验证

默认关闭速率限流，保留第 6 步吞吐实验的可复现性；第 6 步高频取消异常仍保留为未解决项。
